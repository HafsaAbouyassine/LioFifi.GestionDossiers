<Window
    x:Class="CPAM.GestionDossiers.DossierDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:CPAM.GestionDossiers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Détails du dossier"
    Width="900"
    Height="600"
    Background="#FFF5F9FF"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">

    <Window.Resources>
        <DropShadowEffect x:Key="ShadowEffect" BlurRadius="10" Opacity="0.2" ShadowDepth="2" />

        <!-- Styles pour l'en-tête du tableau -->
        <Style x:Key="TableHeaderStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FF1E3A5F" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="5" />
        </Style>

        <!-- Styles pour les cellules du tableau -->
        <Style x:Key="TableCellStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FF2F4F4F" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="5" />
        </Style>

        <!-- Style pour TextBox éditables -->
        <Style x:Key="EditableTextBoxStyle" TargetType="TextBox">
            <Setter Property="BorderBrush" Value="#FFA7C4FF" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="White" />
            <Setter Property="Foreground" Value="#FF2F4F4F" />
            <Setter Property="Padding" Value="5" />
            <Setter Property="Margin" Value="5" />
        </Style>

        <!-- Style pour ComboBox éditables -->
        <Style x:Key="EditableComboBoxStyle" TargetType="ComboBox">
            <Setter Property="BorderBrush" Value="#FFA7C4FF" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="White" />
            <Setter Property="Foreground" Value="#FF2F4F4F" />
            <Setter Property="Padding" Value="5" />
            <Setter Property="Margin" Value="5" />
        </Style>

        <!-- Style pour ListBox éditables -->
        <Style x:Key="EditableListBoxStyle" TargetType="ListBox">
            <Setter Property="BorderBrush" Value="#FFA7C4FF" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="White" />
            <Setter Property="Foreground" Value="#FF2F4F4F" />
            <Setter Property="Padding" Value="5" />
            <Setter Property="Margin" Value="5" />
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF1E3A5F" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Effect" Value="{StaticResource ShadowEffect}" />
            <Setter Property="Margin" Value="5,0" />
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  En-tête  -->
        <Border
            Grid.Row="0"
            Padding="15"
            Background="#FF1E3A5F"
            CornerRadius="5"
            Effect="{StaticResource ShadowEffect}">
            <StackPanel Orientation="Horizontal">
                <TextBlock
                    Margin="0,0,10,0"
                    VerticalAlignment="Center"
                    FontSize="24"
                    Text="📋" />
                <StackPanel>
                    <TextBlock
                        FontSize="18"
                        FontWeight="Bold"
                        Foreground="White"
                        Text="FICHE DOSSIER" />
                    <TextBlock
                        FontSize="14"
                        Foreground="#FFA7C4FF"
                        Text="{Binding NumeroIdentite, StringFormat='N°: {0}'}" />
                </StackPanel>
            </StackPanel>
        </Border>

        <!--  Tableau des détails  -->
        <Border
            Grid.Row="1"
            Margin="0,15,0,0"
            Padding="15"
            Background="White"
            CornerRadius="5"
            Effect="{StaticResource ShadowEffect}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="200" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!--  Ligne 1  -->
                    <TextBlock
                        Grid.Row="0"
                        Grid.Column="0"
                        Style="{StaticResource TableHeaderStyle}"
                        Text="Nom de l'assuré:" />
                    <TextBlock
                        Grid.Row="0"
                        Grid.Column="1"
                        Style="{StaticResource TableCellStyle}"
                        Text="{Binding NomAssure}" />

                    <TextBlock
                        Grid.Row="0"
                        Grid.Column="2"
                        Style="{StaticResource TableHeaderStyle}"
                        Text="Numéro de dossier:" />
                    <TextBlock
                        Grid.Row="0"
                        Grid.Column="3"
                        Style="{StaticResource TableCellStyle}"
                        Text="{Binding NumeroIdentite}" />

                    <!--  Ligne 2  -->
                    <TextBlock
                        Grid.Row="1"
                        Grid.Column="0"
                        Style="{StaticResource TableHeaderStyle}"
                        Text="Date de création:" />
                    <TextBlock
                        Grid.Row="1"
                        Grid.Column="1"
                        Style="{StaticResource TableCellStyle}"
                        Text="{Binding DateCreation, StringFormat='dd/MM/yyyy'}" />

                    <TextBlock
                        Grid.Row="1"
                        Grid.Column="2"
                        Style="{StaticResource TableHeaderStyle}"
                        Text="Date de naissance:" />
                    <TextBlock
                        Grid.Row="1"
                        Grid.Column="3"
                        Style="{StaticResource TableCellStyle}"
                        Text="{Binding DateNaissance, StringFormat='dd/MM/yyyy'}" />

                    <!--  Ligne 3  -->
                    <TextBlock
                        Grid.Row="2"
                        Grid.Column="0"
                        Style="{StaticResource TableHeaderStyle}"
                        Text="Statut:" />
                    <!-- Ligne 3 - ComboBox avec le bon style -->
                    <ComboBox
                        Grid.Row="2"
                        Grid.Column="1"
                        SelectedValue="{Binding Statut, Mode=TwoWay}"
                        SelectedValuePath="Content"
                        Style="{StaticResource EditableComboBoxStyle}">
                        <ComboBoxItem>Nouveau</ComboBoxItem>
                        <ComboBoxItem>En cours</ComboBoxItem>
                        <ComboBoxItem>Terminé</ComboBoxItem>
                        <ComboBoxItem>En attente</ComboBoxItem>
                    </ComboBox>

                    <!-- Catégorie -->
                    <ComboBox
                        Grid.Row="2"
                        Grid.Column="3"
                        SelectedValue="{Binding Categorie, Mode=TwoWay}"
                        SelectedValuePath="Content"
                        Style="{StaticResource EditableComboBoxStyle}">
                        <ComboBoxItem>Médical</ComboBoxItem>
                        <ComboBoxItem>Hospitalisation</ComboBoxItem>
                        <ComboBoxItem>Pharmacie</ComboBoxItem>
                        <ComboBoxItem>Dentaire</ComboBoxItem>
                    </ComboBox>

                    <!-- Agent responsable -->
                    <ComboBox
                        Grid.Row="3"
                        Grid.Column="1"
                        SelectedValue="{Binding AgentResponsable, Mode=TwoWay}"
                        SelectedValuePath="Content"
                        Style="{StaticResource EditableComboBoxStyle}">
                        <ComboBoxItem>Agent Dupont</ComboBoxItem>
                        <ComboBoxItem>Agent Martin</ComboBoxItem>
                        <ComboBoxItem>Agent Lambert</ComboBoxItem>
                    </ComboBox>

                    <!-- Ligne 5 - TextBox avec le bon style -->
                    <TextBox
    Grid.Row="4"
    Grid.Column="1"
    Style="{StaticResource EditableTextBoxStyle}"
    Text="{Binding MontantRembourse, Mode=TwoWay, StringFormat={}{0:C}}" />

                    <TextBox
    Grid.Row="4"
    Grid.Column="3"
    Style="{StaticResource EditableTextBoxStyle}"
    Text="{Binding PourcentageRemboursement, Mode=TwoWay}" />

                    <!-- Ligne 6 - TextBox avec le bon style -->
                    <TextBox
    Grid.Row="5"
    Grid.Column="1"
    Grid.ColumnSpan="3"
    MinHeight="80"
    AcceptsReturn="True"
    Style="{StaticResource EditableTextBoxStyle}"
    Text="{Binding Commentaires, Mode=TwoWay}"
    VerticalScrollBarVisibility="Auto" />

                    <!-- Ligne 7 - ListBox avec le bon style -->
                    <ListBox
    Grid.Row="6"
    Grid.Column="1"
    Grid.ColumnSpan="3"
    MinHeight="60"
    BorderThickness="0"
    ItemsSource="{Binding Documents}"
    Style="{StaticResource EditableListBoxStyle}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Margin="2" Text="{Binding NomFichier}" />
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </ScrollViewer>
        </Border>

        <!--  Boutons d'action  -->
        <Border
            Grid.Row="2"
            Margin="0,15,0,0"
            Padding="10"
            Background="White"
            CornerRadius="5"
            Effect="{StaticResource ShadowEffect}">
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                <Button
                    x:Name="btnAnnuler"
                    Background="#FF7F9FBF"
                    Click="BtnAnnuler_Click"
                    Content="Annuler"
                    Style="{StaticResource PrimaryButtonStyle}" />
                <Button
                    x:Name="btnEnregistrer"
                    Click="BtnEnregistrer_Click"
                    Content="Enregistrer modifications"
                    Style="{StaticResource PrimaryButtonStyle}" />
            </StackPanel>
        </Border>
    </Grid>
</Window>